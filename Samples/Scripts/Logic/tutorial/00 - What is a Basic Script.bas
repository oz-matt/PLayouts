' Sample 00: What is a Basic Script.BAS
' 
' This sample demonstrates what is a PADS Logic Basic Script.
' 
' The code below (in blue and black) defines a Basic script (which does nothing). 
' For information on the meaning on each keyword, click on the keyword and press F1.
' The PADS Logic Basic Editor colors the text depending on what the
' text represents: green for comments, light blue for Basic keywords,
' dark blue for PADS Logic OLE members, black for user variables, red for errors.
'
' Tu run a Basic script, press the Run Button (the green arrow button located in the
' PADS Logic Basic Editor toolbar).
'
' For more details, please refer to the PADS Logic Basic Editor Help File.
'
Sub Main
	
End Sub
